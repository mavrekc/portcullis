services:
  app:
    build: .
    image: portcullis:dev
    environment:
      - PORTCULLIS_LISTEN_PORT=8080
    ports:
      - "8080:8080"

  # victim-agent: a sacrificial demo agent used by the red-team harness (docs/adr, P4 in
  # ROADMAP.md) to replay public injection-benchmark attacks against portcullis with
  # policy enforcement off vs on, so attack-success-rate deltas can be measured.
  # enabled at P4 red-team milestone
  # victim-agent:
  #   build: ./victim-agent
  #   depends_on:
  #     - app
  #   environment:
  #     - PORTCULLIS_PROXY_URL=http://app:8080
